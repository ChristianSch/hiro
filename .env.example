# The only required application setting for local development.
# docker-compose.yml uses the matching development-only hiro/hiro credentials.
HIRO_DATABASE_URL=postgresql://hiro:hiro@127.0.0.1:51432/hiro

# Each service has its own optional configuration namespace. See README.md.
