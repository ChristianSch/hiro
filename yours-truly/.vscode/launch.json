{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch YT",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/server.go",
            "cwd": "${workspaceRoot}"
        }
    ]
}
